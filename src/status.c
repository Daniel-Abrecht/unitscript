#include <stdio.h>
#include <US/unitscript.h>

int us_status( us_unitscript_t* unit, enum us_status* status ){
  (void)unit;
  (void)status;
  puts("us_start: TODO");
  return 0;
}

int us_print_status( us_unitscript_t* unit ){
  (void)unit;
  puts("us_print_status: TODO");
  return 0;
}
